
<!-- Page Title -->
{% set image = header.headerBackground.one() %}
    <section class="page-title style-two"{% if image %} style="background-image:url({{ image.getUrl('header') }})" {% endif %}>
        <div class="auto-container">
			<h1>{{ entry.title }}</h1>
			<ul class="bread-crumb clearfix">
				<li><a href="/">Home</a></li>
				<li>{{ entry.title }}</li>
			</ul>
        </div>
    </section>
    <!-- End Page Title -->